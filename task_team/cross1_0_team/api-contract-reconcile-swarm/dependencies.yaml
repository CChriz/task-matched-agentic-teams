# dependencies.yaml — Swarm Skill startup dependency check
#
# Populated from Stage 2 auto-matching. Skills/tools discovered:
#   Local skills: teamskill-creator (not relevant to this team)
#   Local tools: python3, git, curl

skills: []

tools:
  - name: python3
    required: false
    purpose: "Run pytest for integration test verification during Stage 3 (verifier). If unavailable, the verifier will skip pytest and report 'manual verification required.'"

  - name: go
    required: false
    purpose: "Compile-check the Go server during Stage 3 verification. If unavailable, the verifier will skip go build and report 'manual compilation check required.'"

  - name: git
    required: false
    purpose: "Optional — enables the verifier to produce a clean diff of changes for the final report."
