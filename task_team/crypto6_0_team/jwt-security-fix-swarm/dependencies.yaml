# dependencies.yaml — JWT Security Fix Swarm startup dependency check
#
# Populated from Stage 2 auto-matching results.
# All entries must match roles[].skills and roles[].tools in SKILL.md frontmatter.

skills: []
# Auto-matching scan result: no locally-installed skills are a domain match for
# JWT security analysis, Python bugfixing, or adversarial security testing.
# Each role operates on inline-persona instructions only — no external skill files needed.

tools:
  - name: python3
    required: true
    purpose: "Required for fix-developer to run pytest and for adversarial-verifier to execute attack scripts"

  - name: pytest
    required: true
    purpose: "Test runner — fix-developer runs after each fix to verify regression safety; verifier runs independently for reproducibility"
