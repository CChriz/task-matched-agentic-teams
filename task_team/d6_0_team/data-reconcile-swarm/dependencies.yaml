# dependencies.yaml — Swarm Skill startup dependency check
#
# Populated from Stage 2 auto-matching results.
# Both `skills:` and `tools:` segments must be present.

skills:
  # No domain-specific skills matched for this team.
  # All roles operate on LLM-native capabilities (code analysis, Python scripting, spec verification).
  # Auto-matching confirmed: dev-analyzer and dev-reviewer are installed but not a dependency —
  # the Inline Personas are self-contained with explicit instructions covering their functions.
  []

tools:
  - name: python
    required: true
    purpose: "Execute reconcile.py, parse JSON input/output, run validation checks on reconciled.json"
  - name: jq
    required: false
    purpose: "Optional JSON diffing and field extraction for the Verifier's structural checks"
