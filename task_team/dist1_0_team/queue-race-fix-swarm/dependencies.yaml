# dependencies.yaml — Queue Race Fix Swarm startup dependency check
#
# Populated from Stage 2 auto-matching results.
# All entries must match roles[].skills and roles[].tools in SKILL.md frontmatter.

skills: []
# Auto-matching scan: no locally-installed skills are a domain match for
# Python concurrency analysis, race condition bugfixing, or concurrent stress testing.
# Each role operates on inline-persona instructions only.

tools:
  - name: python3
    required: true
    purpose: "Required for fix-developer to run pytest and for adversarial-tester to execute concurrent stress tests"

  - name: pytest
    required: true
    purpose: "Test runner — developer runs after each fix; tester runs independently to verify reproducibility and 10K stress test"
