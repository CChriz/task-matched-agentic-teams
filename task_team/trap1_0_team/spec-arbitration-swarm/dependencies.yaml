# dependencies.yaml — Spec Arbitration Swarm

skills: []
# Auto-matching: no locally-installed skills match OpenAPI spec conflict arbitration.

tools:
  - name: python3
    required: true
    purpose: "Required for running pytest and the API server for endpoint testing"
  - name: pytest
    required: true
    purpose: "Test runner — developer runs to verify backward compatibility; verifier runs independently"
